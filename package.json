{
  "name": "audio-separation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon backend/server.js",
    "build": "npm install && npm install --prefix frontend && npm run create-env && npm run build --prefix frontend",
    "create-env": "conda env create -n myenv -f=environment.yml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.5",
    "child_process": "^1.0.2",
    "cloudinary": "^1.41.3",
    "cors": "^2.8.5",
    "express-async-handler": "^1.2.0",
    "multer": "^1.4.5-lts.1",
    "spleeter": "^0.1.0"
  }
}
